////////////////////////////////////////////////////1. Ders/////////////////////////////////////////////

!! C dilinde call by referance yapmanın tek yolu pointerlardır

void swap(int *p1, int *p2)
{
int temp = *p1;
*p1 = *p2;
*p2 = temp;
}

int main(void)
{
int x = 45;
int y = 90;

swap(&x, &y);
printf("x = %d  y = %d\n", x, y);
}

//25:36









